pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                echo 'Checking out code...'
            }
        }

        stage('Run Python Script') {
            steps {
                sh 'login test case.py'
            }
        }
    }
}
