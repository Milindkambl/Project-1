pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                echo 'Checking out code...'
            }
        }

        stage('Run Python Script') {
            steps {
                sh 'python3 your_script.py'
            }
        }
    }
}
